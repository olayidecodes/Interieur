import { images } from '../constants'

const Footer = () => {
  return (
    <section className='section__padding footer'>
        <h1>Start Project</h1>
        <img src={images.bottom} alt="Start project" />
    </section>
  )
}

export default Footer